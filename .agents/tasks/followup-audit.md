# 추가 오류 점검과 수정·푸시

- 목표와 완료 조건: 추가 개선점과 재현 가능한 버그를 수정하고 회귀·실제 실행·독립 검토 후 origin/main에 커밋·푸시.
- 담당 도구·세션: root 통합·브라우저 QA·문서·Git, audit_engine Python 구현/브라우저 리뷰, design_references 브라우저 구현/UI·보고서 리뷰, ui_shell UI 구현/Python 리뷰.
- 상태: 완료
- 담당 파일·디렉터리: apps/server, apps/web, packages/engine, tests, package.json·lockfile, README와 docs, 프로젝트 정보·이 기록.
- 브랜치·기준 커밋: main / d6b00e6. 시작 시 clean, origin/main 일치. 최종 Git 상태와 원격 SHA는 실제 ref로 확인한다.

## 결정 사항

- 사용자 요청에 commit/push가 포함된다. 관련 파일만 명시적으로 stage하고 origin/main에 일반 push한다.
- 기존 사용자 데이터·페이지·모델은 보존한다. 브라우저 QA는 별도 출처에서 수행하고 fixture와 실제 모델 성공을 구분했다.
- 작성자 외 독립 리뷰를 영역별로 수행했다. Node 25 지원 범위 지적은 최종 26+로 수정·재확인했다.

## 변경한 내용

- 페이지 이탈 시 다음 작업 실행, 잘못된 전체 모델 응답의 체크포인트 삭제, 다중 탭 삭제 경합을 수정했다.
- 지연된 상세 응답의 최신 상태 덮어쓰기, 페이지 전환 중 이전 목록·잘못된 빈 상태, 취소 완료로 오인하는 안내를 수정했다.
- 업로드 요청 취소 시 파일 잔류·열린 임시 파일, 장식 단락 음성 합성, 단독 surrogate JSON의 CLI 예외/API 500을 수정했다.
- Node 지원 범위를 잠금 의존성의 교집합으로 맞췄다. 상세 결과는 docs/followup-audit.md에 기록했다.

## 검증

- Python 전체 103개, 웹 Vitest 95개와 SW Node 6개 통과.
- Node 22.20.0에서 타입·포맷·웹 테스트·프로덕션 빌드 통과.
- Ruff 전체 lint/format, uv lock 확인·전체 패키지 빌드, AI 환경 연결, diff 검사 통과.
- 실제 Supertonic 신규/캐시 재개·원문 보존·PCM 해시·MP3 전체 디코딩 통과.
- 최종 브라우저 앱 탭 종료 후 6개 저장 구간 유지, 재개 시 6개 재사용·12구간 완성, 대기 작업 후속 실행 확인.
- 브라우저 네트워크 차단을 전후 요청 실패로 확인한 상태에서 신규 8.2초 음성 생성·MP3 저장·전체 디코딩 통과.
- 실제 UI 지연 응답 재현, 메뉴 이동과 요청 실패의 입력 보존, 배속 검증, 성공 후 초기화, 파일/PDF 범위 유지, 390px 모바일 가로 넘침 없음 확인.
- 브라우저·Python·UI와 보고서 독립 검토 완료. 로컬 원시 증거는 output/followup-audit/에 있으며 Git에서 제외한다.
- 미검증 범위: 모든 브라우저·실제 CDN 장애·운영체제 전체 네트워크 차단. BFCache 이벤트 경합과 HTTP 이상 응답은 명시적 fixture 검증이다.

## 다음 작업·질문

- 없음. 서버 배포는 이번 요청 범위에 포함하지 않았다.
